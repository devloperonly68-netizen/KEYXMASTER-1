KEYxMASTER BOT CONFIGURATION

Bot name: KEYxMASTER
Bot username: @X_Panel_Store_bot
Admin ID: 7088682169
ZAP key: set via Railway variable ZAP_KEY

The Telegram bot token is intentionally not stored in this export. TeleBotHost injects bot.token at runtime after the bot is connected in the hosting panel. Paste the token into the hosting panel's bot connection/credentials field only, not into command files or this archive.

Support and payment-proof URLs remain unchanged because no replacement URLs were provided. Set them with the bot's admin commands after import if needed.
